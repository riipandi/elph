Generate and maintain a living code wiki for the Elph Rust workspace (edition 2024, resolver v2, Rust 1.97) at `github.com/riipandi/elph`. Create pages under `openwiki/` grounded in actual repository structure, `pub mod` exports from each crate's `lib.rs`, Cargo.toml feature flags, and recent git history — not generic AI-agent concept Structure: `quickstart.md` (workspace layout, make targets, crate dependency graph), `architecture/overview.md` (crate dependency graph, agent loop phases, session persistenc ), `architecture/source-map.md` (crate-by-crate module map with file paths, noting which modules are pi ports vs elph-only), `workflows/agent-loop.md` (turn cycle: `AgentHarness::prompt()` → `run_agent_loop()` → tool execution → compaction), `workflows/compaction.md` (context window management, summarization, timestamp-gated estimate), `workflows/auth.md` (CredentialStore, ModelsStore, OAuth providers, resolve_provider_auth), `domains/providers.md` (ProviderStreams trait, 30+ provider adapters, compat flags, ThinkingLevel, constrainedSampling, SessionAffinityFormat), `domains/mcp.md` (McpRegistry, transports, AES-256-GCM encryption, tool naming), `domains/tools.md` (AgentTool, built-in tools, feature flags), `domains/skills.md` (SKILL.md format, resolution, MiniJinja templates), `operations.md` (CLI subcommands, ELPH_ env config paths, observability), `testing.md` (unit/integration test patterns, live provider tests), `integrations/pi-port.md` (upstream commit cee5ff75, crate mapping, parity gaps, elph-only extensions). Anchor ever actual code — check `git log --oneline` for module history, cite commits, note `#[cfg(feature)]` gates, reference upstream TypeScript paths for pi ports. Avoid generic AI descriptions, speculative architecture, verbatim doc comments, and references to the removed `elph-core` crate. Prefer code snippets, module paths, and short explanatio over prose paragraphs. Cross-link between pages instead of duplicating content.

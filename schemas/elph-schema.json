{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "$id": "https://elph.space/elph-schema.json",
    "title": "Elph settings configuration",
    "description": "User preferences for the Elph coding agent, grouped by domain (ui, models, memory, notifications, compaction). Active model, thinking level, and agent mode are per-session (not stored here). Home: ~/.elph/settings.json. Optional project overrides: <workDir>/.elph/settings.json (same shape). Merge: defaults \u2190 home \u2190 project (project wins per nested key). Runtime saves write the home layer only. Legacy session group and flat keys are migrated on load; unknown keys are ignored. Top-level transport: maxRetries, defaultTimeout. Legacy nested provider group is migrated on load.",
    "type": "object",
    "additionalProperties": false,
    "properties": {
        "$schema": {
            "type": "string",
            "description": "JSON Schema URL (editor autocompletion). Ignored at runtime."
        },
        "preferredChatLanguage": {
            "type": "string",
            "default": "english",
            "description": "Preferred language for AI chat responses in the transcript. Code, comments, and documentation remain in English."
        },
        "maxRetries": {
            "type": "integer",
            "minimum": 0,
            "default": 2,
            "description": "Retries on 5xx / network errors for LLM HTTP transport."
        },
        "defaultTimeout": {
            "type": "string",
            "default": "120s",
            "description": "Inactivity and SSE stall limit for LLM streams (e.g. 120s, 2m, 1h, or milliseconds as digits)."
        },
        "ui": {
            "$ref": "#/definitions/ui"
        },
        "models": {
            "$ref": "#/definitions/models"
        },
        "memory": {
            "$ref": "#/definitions/memory"
        },
        "notifications": {
            "$ref": "#/definitions/notifications"
        },
        "compaction": {
            "$ref": "#/definitions/compaction"
        }
    },
    "definitions": {
        "ui": {
            "type": "object",
            "additionalProperties": false,
            "description": "Transcript, chrome, and picker presentation.",
            "properties": {
                "theme": {
                    "type": "string",
                    "default": "auto",
                    "enum": ["auto", "dark", "light"],
                    "description": "Appearance: auto follows terminal via COLORFGBG; dark/light force a base palette."
                },
                "themes": {
                    "$ref": "#/definitions/themePalettes",
                    "description": "Optional per-appearance color token overrides (partial maps)."
                },
                "showThinking": {
                    "type": "boolean",
                    "default": true,
                    "description": "Stream model reasoning / thinking blocks in the TUI."
                },
                "autoExpandThinking": {
                    "type": "boolean",
                    "default": false,
                    "description": "When true, thinking blocks start expanded in the transcript."
                },
                "stickyScroll": {
                    "type": "boolean",
                    "default": true,
                    "description": "Pin the latest user prompt while scrolling assistant replies."
                },
                "footerTokenDisplay": {
                    "type": "string",
                    "default": "both",
                    "enum": ["both", "percentage", "count"],
                    "description": "How context token usage is shown in the status footer."
                },
                "coloredStatusFooter": {
                    "type": "boolean",
                    "default": true,
                    "description": "When true, footer status uses mode/thinking/git accent colors; otherwise dimmed grey."
                },
                "filePicker": {
                    "$ref": "#/definitions/filePicker"
                },
                "allowModeChangeWhileBusy": {
                    "type": "boolean",
                    "default": true,
                    "description": "When true, allow mode changes (keyboard shortcut and agent request) while the agent is busy streaming or running tools."
                }
            }
        },
        "themePalettes": {
            "type": "object",
            "additionalProperties": false,
            "description": "Dark and light token maps. Each map is a partial UiTheme color override.",
            "properties": {
                "dark": {
                    "$ref": "#/definitions/themeTokens"
                },
                "light": {
                    "$ref": "#/definitions/themeTokens"
                }
            }
        },
        "themeTokens": {
            "type": "object",
            "additionalProperties": false,
            "description": "Partial color overrides. Values: #RGB/#RRGGBB, rgb(r,g,b), 'r,g,b', or named colors (white, reset, \u2026).",
            "properties": {
                "textPrimary": {
                    "$ref": "#/definitions/colorValue"
                },
                "textSecondary": {
                    "$ref": "#/definitions/colorValue"
                },
                "textMuted": {
                    "$ref": "#/definitions/colorValue"
                },
                "textHint": {
                    "$ref": "#/definitions/colorValue"
                },
                "accent": {
                    "$ref": "#/definitions/colorValue"
                },
                "accentSoft": {
                    "$ref": "#/definitions/colorValue"
                },
                "border": {
                    "$ref": "#/definitions/colorValue"
                },
                "borderFocus": {
                    "$ref": "#/definitions/colorValue"
                },
                "borderSubtle": {
                    "$ref": "#/definitions/colorValue"
                },
                "shellBorder": {
                    "$ref": "#/definitions/colorValue"
                },
                "shellBorderDimmed": {
                    "$ref": "#/definitions/colorValue"
                },
                "surface": {
                    "$ref": "#/definitions/colorValue"
                },
                "codeBlockBg": {
                    "$ref": "#/definitions/colorValue"
                },
                "selectionBg": {
                    "$ref": "#/definitions/colorValue"
                },
                "dialogSelectionBg": {
                    "$ref": "#/definitions/colorValue"
                },
                "success": {
                    "$ref": "#/definitions/colorValue"
                },
                "warning": {
                    "$ref": "#/definitions/colorValue"
                },
                "error": {
                    "$ref": "#/definitions/colorValue"
                }
            }
        },
        "colorValue": {
            "description": "iocraft Color as hex (#RRGGBB), rgb()/rgba(), r,g,b CSV, named color, [r,g,b], or {r,g,b}.",
            "oneOf": [
                {
                    "type": "string",
                    "minLength": 1
                },
                {
                    "type": "array",
                    "minItems": 3,
                    "maxItems": 4,
                    "items": {
                        "type": "number"
                    }
                },
                {
                    "type": "object",
                    "additionalProperties": false,
                    "required": ["r", "g", "b"],
                    "properties": {
                        "r": {
                            "type": "number"
                        },
                        "g": {
                            "type": "number"
                        },
                        "b": {
                            "type": "number"
                        }
                    }
                }
            ]
        },
        "filePicker": {
            "type": "object",
            "additionalProperties": false,
            "description": "@-mention file picker preferences.",
            "properties": {
                "showHiddenFiles": {
                    "type": "boolean",
                    "default": false,
                    "description": "When true, @ file search includes dotfiles and dot-directories."
                }
            }
        },
        "models": {
            "type": "object",
            "additionalProperties": false,
            "description": "Model catalog preferences and seeds for new sessions (not live session state).",
            "properties": {
                "defaultModel": {
                    "type": "string",
                    "minLength": 1,
                    "description": "Optional provider/model_id seed for new sessions only (e.g. opencode/big-pickle). Live model is per-session."
                },
                "sessionTitleModel": {
                    "type": "string",
                    "default": "inherit",
                    "description": "Model for session title generation. Use provider/model_id or \"inherit\" for the session model."
                },
                "compactionModel": {
                    "type": "string",
                    "default": "inherit",
                    "description": "Model used when summarizing history during compaction. \"inherit\" uses the session model."
                },
                "treeBranchSummaries": {
                    "type": "string",
                    "default": "inherit",
                    "description": "Model for tree branch summarization. \"inherit\" uses the session model."
                },
                "defaultThinkingLevel": {
                    "type": "string",
                    "default": "high",
                    "enum": ["off", "minimal", "low", "medium", "high", "xhigh", "max"],
                    "description": "Thinking/reasoning level seed for new sessions only. Live level is per-session."
                },
                "showConfiguredOnly": {
                    "type": "boolean",
                    "default": false,
                    "description": "When true, the model picker All list and Provider tabs only show providers with credentials in auth.json."
                },
                "scopedModels": {
                    "type": "array",
                    "default": [],
                    "items": {
                        "type": "string",
                        "minLength": 1,
                        "description": "provider/model_id entry (e.g. opencode/big-pickle)."
                    },
                    "description": "Models for Ctrl+P cycling and the Scoped tab. Edit via /scoped-models."
                }
            }
        },
        "memory": {
            "type": "object",
            "additionalProperties": false,
            "description": "Local embedding / floppy memory preferences.",
            "properties": {
                "embedModel": {
                    "type": "string",
                    "default": "AllMiniLML6V2",
                    "minLength": 1,
                    "description": "Embedding model catalog name or Hugging Face repo id."
                },
                "embedQuantized": {
                    "type": "boolean",
                    "default": true,
                    "description": "Prefer quantized ONNX weights when a *Q variant exists."
                }
            }
        },
        "notifications": {
            "type": "object",
            "additionalProperties": false,
            "description": "Desktop notification preferences. Controls which events trigger native OS notifications (macOS Notification Center, Linux D-Bus, Windows Toast).",
            "properties": {
                "enabled": {
                    "type": "boolean",
                    "default": true,
                    "description": "Master switch \u2014 disable all desktop notifications."
                },
                "onTurnComplete": {
                    "type": "boolean",
                    "default": true,
                    "description": "Notify when the agent finishes a turn."
                },
                "onToolPermission": {
                    "type": "boolean",
                    "default": true,
                    "description": "Notify when the agent requests tool permission."
                },
                "onUserQuestion": {
                    "type": "boolean",
                    "default": true,
                    "description": "Notify when the agent asks a question."
                },
                "onError": {
                    "type": "boolean",
                    "default": true,
                    "description": "Notify on errors (agent / MCP / bootstrap failure)."
                },
                "onTurnCancel": {
                    "type": "boolean",
                    "default": false,
                    "description": "Notify when a running turn is canceled."
                },
                "onStartupReady": {
                    "type": "boolean",
                    "default": true,
                    "description": "Notify when bootstrap / startup completes."
                },
                "minTurnDurationSecs": {
                    "type": "number",
                    "default": 5.0,
                    "minimum": 0,
                    "description": "Minimum turn duration (seconds) before sending a turn-complete notification. Prevents noise from quick turns."
                },
                "appName": {
                    "type": "string",
                    "default": "Elph",
                    "minLength": 1,
                    "description": "Application name shown in the notification banner."
                }
            }
        },
        "compaction": {
            "type": "object",
            "additionalProperties": false,
            "description": "Auto-compaction thresholds. Automatic compaction always runs after turns when usage exceeds the threshold; manual /compact is always available. There is no enabled kill-switch.",
            "properties": {
                "thresholdPct": {
                    "type": "integer",
                    "minimum": 1,
                    "maximum": 100,
                    "default": 80,
                    "description": "Context-window usage percentage (1\u2013100) that triggers compaction. Compact when context tokens exceed contextWindow * thresholdPct / 100."
                },
                "keepRecentTokens": {
                    "type": "integer",
                    "minimum": 0,
                    "default": 20000,
                    "description": "Number of recent tokens to retain after compaction."
                }
            }
        }
    }
}

{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "Elph version metadata",
    "description": "Release and sync metadata persisted at ~/.local/share/elph/version.json.",
    "type": "object",
    "properties": {
        "lastSyncProviders": {
            "type": "string",
            "description": "RFC3339 timestamp of the last successful models.dev provider metadata sync."
        },
        "relaseCheckedAt": {
            "type": "string",
            "description": "RFC3339 timestamp of the last release check."
        },
        "stableVersion": {
            "type": "string",
            "description": "Latest known stable release version."
        },
        "version": {
            "type": "string",
            "description": "Installed or reported Elph version."
        }
    },
    "additionalProperties": false
}

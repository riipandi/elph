# yaml-language-server: $schema=https://json.schemastore.org/github-action.json

name: setup-rust
description: Prepare Rust toolchain, caches, and CI tools

inputs:
  sharedKey:
    description: Cache key prefix (e.g. ci-ubuntu-24.04). Ignored when cacheBackend is namespace.
    required: false
    default: ci
  cacheBackend:
    description: rust-cache (GitHub Actions cache), namespace (Cache Volume + Namespace sccache), or avrea (GitHub Actions cache + Avrea WebDAV sccache).
    required: false
    default: rust-cache
  installNextest:
    description: Install cargo-nextest for running tests
    required: false
    default: "true"
  installSccache:
    description: Install sccache (needed when cacheBackend is namespace or avrea)
    required: false
    default: "false"
  saveCache:
    description: "Whether to save the cargo cache (default: true). Ignored for namespace."
    required: false
    default: "true"

runs:
  using: composite
  steps:
    - name: Install toolchain
      uses: dtolnay/rust-toolchain@v1
      with:
        toolchain: 1.97.0
        components: clippy, rustfmt

    - name: Install tools
      if: inputs.installNextest == 'true' || inputs.installSccache == 'true' || inputs.cacheBackend == 'namespace' || inputs.cacheBackend == 'avrea' || runner.os == 'Linux'
      shell: bash
      run: |
        set -euo pipefail
        export BINSTALL_DISABLE_TELEMETRY=true
        if ! command -v cargo-binstall >/dev/null 2>&1; then
          curl -L --proto '=https' --tlsv1.2 -sSf \
            https://raw.githubusercontent.com/cargo-bins/cargo-binstall/main/install-from-binstall-release.sh | bash
        fi
        if [ "${{ inputs.installNextest }}" = "true" ] && ! command -v cargo-nextest >/dev/null 2>&1; then
          cargo binstall --locked -y cargo-nextest
        fi
        if [ "${{ inputs.installSccache }}" = "true" ] || [ "${{ inputs.cacheBackend }}" = "namespace" ] || [ "${{ inputs.cacheBackend }}" = "avrea" ]; then
          if ! command -v sccache >/dev/null 2>&1; then
            cargo binstall --locked -y sccache
          fi
        fi

    - name: Cache Cargo
      if: inputs.cacheBackend == 'rust-cache'
      uses: swatinem/rust-cache@v2.9.1
      with:
        cache-on-failure: true
        shared-key: ${{ inputs.sharedKey }}
        cache-bin: false
        save-if: ${{ inputs.saveCache }}

    - name: Cache Cargo (volume)
      if: inputs.cacheBackend == 'namespace'
      uses: namespacelabs/nscloud-cache-action@v1
      with:
        cache: rust

    - name: Cache Cargo (Avrea)
      if: inputs.cacheBackend == 'avrea'
      uses: swatinem/rust-cache@v2.9.1
      with:
        cache-on-failure: true
        shared-key: ${{ inputs.sharedKey }}
        cache-bin: false
        save-if: ${{ inputs.saveCache }}

    - name: Configure sccache
      if: inputs.cacheBackend == 'namespace'
      shell: bash
      run: |
        set -euo pipefail
        nsc cache sccache setup --cache_name default >> "$GITHUB_ENV"
        {
          echo "RUSTC_WRAPPER=sccache"
          echo "CARGO_TERM_COLOR=always"
          echo "CARGO_INCREMENTAL=0"
          echo "SCCACHE_DIRECT=true"
          echo "SCCACHE_MAXSIZE=50GB"
        } >> "$GITHUB_ENV"

    - name: Configure sccache (Avrea)
      if: inputs.cacheBackend == 'avrea'
      shell: bash
      run: |
        set -euo pipefail
        {
          echo "RUSTC_WRAPPER=sccache"
          echo "SCCACHE_WEBDAV_ENDPOINT=http://cache.avrea.com:8290/sccache-build/webdav"
          echo "SCCACHE_WEBDAV_KEY_PREFIX="
          echo "CARGO_TERM_COLOR=always"
          echo "CARGO_INCREMENTAL=0"
          echo "SCCACHE_MAXSIZE=50GB"
        } >> "$GITHUB_ENV"
        sccache --start-server

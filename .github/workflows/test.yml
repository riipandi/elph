# yaml-language-server: $schema=https://json.schemastore.org/github-workflow.json

name: Test

on:
  workflow_call:
    inputs:
      app:
        description: Application to test (elph)
        required: false
        type: string
        default: elph
      sharedKeyPrefix:
        description: Cache key prefix
        required: false
        type: string
        default: ci
      mode:
        description: full = fmt+check+lint+test+debug build; check = fmt+check+lint
        required: false
        type: string
        default: full
  push:
    branches: [main]
    paths:
      - "crates/coding-agent/**"
      - "crates/elph-agent/**"
      - "crates/elph-ai/**"
      - "crates/elph-tui/**"
      - "Cargo.toml"
      - "Cargo.lock"
      - "rust-toolchain.toml"
      - "Makefile"
      - ".github/**"
  pull_request:
    paths:
      - "crates/coding-agent/**"
      - "crates/elph-agent/**"
      - "crates/elph-ai/**"
      - "crates/elph-tui/**"
      - "Cargo.toml"
      - "Cargo.lock"
      - "rust-toolchain.toml"
      - "Makefile"
      - ".github/**"

permissions:
  contents: read

concurrency:
  group: test-${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  # Windows GitHub-hosted compiles (wasmtime) often exceed the gap between main
  # pushes. Cancelling those runs also drops rust-cache, so the next job starts cold.
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

env:
  CI: true
  BINSTALL_DISABLE_TELEMETRY: true

jobs:
  test:
    name: ${{ matrix.name }}${{ matrix.shard && format(' ({0})', matrix.shard) || '' }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: linux
            runner: namespace-profile-linux-base-amd64
            cacheBackend: namespace
          - name: macos
            runner: namespace-profile-macos-base-arm64
            cacheBackend: namespace
          # Windows GitHub-hosted runners are 4 vCPU / 16 GB (vs 8/6 on Namespace),
          # so the pipeline is core-bound. Shard the nextest run across two runners
          # to parallelize both compilation and execution, roughly halving wall time.
          - name: windows
            runner: windows-latest
            cacheBackend: rust-cache
            timeoutMinutes: 90
            shard: "1/2"
          - name: windows
            runner: windows-latest
            cacheBackend: rust-cache
            timeoutMinutes: 90
            shard: "2/2"
    runs-on: ${{ matrix.runner }}
    timeout-minutes: ${{ matrix.timeoutMinutes || 45 }}
    env:
      # sccache via the GitHub Actions cache backend (no R2/S3) — Windows only.
      SCCACHE_GHA_ENABLED: ${{ matrix.name == 'windows' && 'true' || '' }}
      CARGO_INCREMENTAL: ${{ matrix.name == 'windows' && '0' || '' }}
    steps:
      - uses: actions/checkout@v7

      - name: Setup Rust
        uses: ./.github/setup-rust
        with:
          sharedKey: ${{ inputs.sharedKeyPrefix || 'ci' }}-${{ inputs.app || 'elph' }}-${{ matrix.name }}
          cacheBackend: ${{ matrix.cacheBackend }}
          saveCache: ${{ github.ref == 'refs/heads/main' || matrix.name == 'windows' }}

      - name: Sccache (GitHub Actions cache)
        if: matrix.name == 'windows'
        uses: mozilla-actions/sccache-action@v0.0.11

      - name: Check format
        shell: bash
        run: cargo fmt --all --check

      - name: Check compile
        shell: bash
        run: make check

      - name: Lint
        shell: bash
        run: make lint

      - name: Lint (elph-agent full features)
        if: matrix.name != 'windows' && (inputs.app || 'elph') == 'elph'
        shell: bash
        run: make lint -- -p elph-agent --features full

      - name: Test
        if: (inputs.mode || 'full') == 'full'
        shell: bash
        run: |
          SHARD=""
          if [ -n "${{ matrix.shard }}" ]; then
            SHARD="--partition count:${{ matrix.shard }}"
          fi
          make test SHARD="$SHARD"

      - name: Test (elph-agent full features)
        if: matrix.name != 'windows' && (inputs.mode || 'full') == 'full' && (inputs.app || 'elph') == 'elph'
        shell: bash
        run: make test -- -p elph-agent --features full

      - name: Build debug
        if: (inputs.mode || 'full') == 'full'
        shell: bash
        run: make build

      - name: Show sccache stats
        if: always()
        uses: ./.github/show-sccache-stats

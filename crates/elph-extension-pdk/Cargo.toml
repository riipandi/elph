[package]
name = "elph-extension-pdk"
version = "0.1.0"
edition = "2024"
publish = false
description = "Guest helpers for Elph wasmi extensions (wasm32-unknown-unknown)"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
